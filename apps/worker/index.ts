const REGION_ID = process.env.REGION_ID
const WORKER_ID = process.env.WORKER_ID

async function main() {
    while (1) {
        //read from the stream



        //process the website and store the result
        //TODO: IT should probably be routed through a queue in a bulk DB request
    }
}




main()